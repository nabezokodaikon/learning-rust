// extern crate closures;
// use simulated_expensive_calculation;

// use crate::closures;

#[cfg(test)]
mod tests {
    // use super::simulated_expensive_calculation;
    // use super::*;

    // #[test]
    // fn call_with_different_values() {
    // // let mut c = Cacher::new(|a| a);
    // // let v1 = c.value(1);
    // // simulated_expensive_calculation(1);
    // }
}
